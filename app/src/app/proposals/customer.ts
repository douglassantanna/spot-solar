export interface Customer {
  customerFullName: string;
  email: string;
  telephoneNumber: string;
}
export interface Address {
  zipCode: string;
  street: string;
  neighborhood: string;
  city: string;
  state: string;
  notes: string;
}
